# Warehouse
Build a warehouse with python and blender

* Use Blender to create the vertical beam and horizontal beam and export the files as an object file
* Use json config file to read 4 different racks
* In Blender use python code to read the json files and objects to build the racks
